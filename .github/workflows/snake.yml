name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: Platane/snk@v3
        with:
          github_user_name: Lesslibls
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake.gif
    # 🐍 Color para la serpiente
        color_snake: "#800080" 
    # ⚫ Color para los puntos (los días de contribución)
        color_dots: "#ff1493"
        
